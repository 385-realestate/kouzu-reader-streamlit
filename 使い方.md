# 公図PDF変換ツール 使い方

公図（法務局の地図）PDFを読み込み、区画線・地番数字・方角記号を自動で読み取って、GeoJSON / DXF / JWC（Jw_cad用）形式に変換するツールです。処理はすべてブラウザ内で行われ、PDFやデータが外部サーバーに送信されることはありません。

---

## 1. 基本的な使い方（5ステップ）

1. **PDFを読み込む** — 左上の「PDFをドロップ／またはクリックして選択」に公図PDFを渡す
2. **縮尺を確認する** — 「JWCAD出力設定」を開き、公図に記載の縮尺（多くは1:500）を選ぶ
3. **「変換実行」ボタンを押す**
   - PDFがベクター形式（デジタル公図）ならすぐに完了
   - スキャン画像（完全に紙をスキャンしたPDF）の場合は、境界線抽出とOCR文字認識が走るため**数十秒〜1分程度**かかる（進捗バーに状況が表示される）
4. **プレビューで確認する** — 画面右側に色分けされた図面が表示される（色の意味は下記「2. 表示色の意味」参照）
5. **必要な形式でダウンロード** — 画面上部の「GeoJSON」「DXF」「JWC」ボタンから出力

---

## 2. 表示色の意味

変換後のプレビュー・DXF/JWC出力とも、内容によって次のように色分けされます。

| レイヤ | 色 | 内容 |
|---|---|---|
| 境界線 | 黄色 | 区画の境界線（読み取れた区画線すべて） |
| 外枠 | 白（青系のプレビュー表示） | 図面の外周枠 |
| 地番 | 緑（一部オレンジ表示になるCADソフトあり） | 地番数字。実際に文字として認識できたもの（実テキスト）と、文字らしき輪郭として色分けしただけのもの（未確定）の両方を含む |

画面右上の「表示: 境界線／外枠／地番／方角」ボタンで、それぞれのレイヤをプレビュー上でON/OFFできます（ダウンロードするファイルの内容にも反映されます）。

> 「方角」ボタンは残っていますが、現在は自動処理を行っていません。方角記号（コンパスマーク）の位置・向きはPDFごとに異なる（道路沿いに斜めに配置される場合もある）ことが分かり、固定位置への自動描画や自動検出は精度上の問題から撤回しました。方角記号も他の線と同様に境界線（黄）として出力されます。

---

## 3. 地番文字の読み取り（OCR）について ― 必ずお読みください

スキャン画像PDF（PDF内に実際の文字データが無いもの）の場合、地番数字は**OCR（Tesseract.js）による自動文字認識**で読み取っています。

- 認識した文字列は、信頼度が高いもの（文字単位でも一定以上の信頼度があり、切り出し直して再認識しても同じ結果になったもの）だけを実際の文字（緑）として採用します
- それ以外は「文字らしき輪郭」として色分けするだけに留め、実際の文字列としては採用しません
- **信頼度が高くても誤読はゼロにはなりません**（特に「1」と「7」の見分けが難しいケースがあります）

> ⚠️ **地番は土地の識別情報です。DXF/JWC出力後は、必ず元のPDF・スキャン画像と地番を目視で照合してください。** 特に重要な取引・法的手続きに使う場合は必須です。

---

## 4. 距離測定・PNG保存（現地確認・商談向け）

> ℹ️ **地番ハイライト機能について**: 実データ検証の結果、区画の境界線検出精度が不十分（フラッドフィル方式の限界により、テスト対象PDFの地番全件でハイライトに失敗）と判明したため、**今回のリリースでは地番検索・ハイライト機能を無効化（非表示）しています**。地番一覧の表示自体は引き続き利用できますが、クリックしても区画はハイライトされません。境界線検出の精度が改善され次第、再度有効化を検討します。

「公図PDFに定規を当てて距離を測る」というアナログ作業をブラウザ上で置き換える機能です。変換実行後、右側のプレビュー画面で使えます。

### 2点間の距離を測る（定規代わり）
- プレビュー右下の 📏 ボタンを押すと測定モードになります（ボタンが赤くなります）。
- 図面上で**2点をクリック**すると、その2点間の実寸距離が m / mm 単位で表示されます（縮尺・解像度から自動計算）。
- もう一度 📏 ボタンを押すと測定モードを終了します。3点目をクリックすると新しい測定として最初からやり直せます。

### 測定結果をPNG画像で保存する
- 画面上部の「🖨 PNG保存」ボタンで、距離測定の表示を含めた現在のプレビューをそのままPNG画像として保存できます。現地確認や商談の場でそのまま印刷・共有できます。
- PDFを読み込み直すと、測定結果はリセットされます。

---

## 5. 画面の設定項目

以前は解像度(DPI)・最小面積・輪郭の精細さ・テキスト抽出・公図レイアウト最適化・薄線強化など多数の調整項目がありましたが、実際のPDFで何度もテストした結果「常にこの設定が正解」と判断できたため、次の3つだけを画面に残し、他は最適な値に固定して非表示にしています。

| 項目 | 説明 |
|---|---|
| 輪郭を色付き表示 | プレビュー上で検出した線を色付きで重ね描きするかどうか（見た目だけの設定） |
| 破線をスキップ（点線除外） | 点線（境界未確定線など）を出力に含めるかどうか。図面の内容そのものが変わるため、目的に応じて切り替えてください |
| 斜線強化（ラスター優先） | ベクター公図で斜めの区画線がうまく読み取れない場合にONにする設定。通常のスキャンPDFでは不要 |

---

## 6. JWCAD出力設定

- **公図の縮尺**：PDFの表題欄（出力縮尺）に記載の値を選んでください。多くの公図は1:500です。ここで選んだ縮尺をもとに、DXF/JWCの座標が実寸mm（実際の距離）に変換されます。

---

## 7. 地理座標変換（GCP）— 通常は不要

ピクセル座標を緯度経度に変換したい場合のみ使う省略可能な機能です。基準点を3点以上入力すると、GeoJSON出力が緯度経度（WGS84）になります。

⚠️ GCPを設定した状態ではDXF/JWC出力はできません（緯度経度をmmとして書き出すと図面が壊れるため、安全のためエラーで停止するようにしています）。DXF/JWCが必要な場合はGCPを設定しない状態でご利用ください。

---

## 8. 出力形式の使い分け

| 形式 | 用途 |
|---|---|
| GeoJSON | 地図システム・プログラムでの二次利用向け |
| DXF | 一般的なCADソフト全般（AutoCAD系等）で開ける形式 |
| JWC | Jw_cad専用のテキスト形式 |

---

## 9. Jw_cadで読み込むときの注意

- 「JWC・DXF読込み」の **「色を初期化する」はチェックを入れないでください**。チェックを入れると、このツールが設定したレイヤ別の色分け（境界線=黄／地番=緑／方角=青）が失われ、すべて同じ色に見えてしまいます。
- 「DXF読込み」の「図面範囲を読み取る」はチェックを入れて問題ありません（図面全体がすぐに表示されます）。
- ツールバーの配置が普段と違う等の症状が出た場合、**このツールが出力するDXF/JWCファイルはJw_cad本体の設定やツールバー配置を変更する情報を持っていません**。原因はJw_cad側の表示設定と考えられます（「表示→ツールバー」から初期配置に戻せる場合があります）。

---

## 10. よくある症状と対処

| 症状 | 対処 |
|---|---|
| 変換に時間がかかる | スキャンPDF＋OCR実行時は数十秒〜1分程度かかります。仕様です |
| 地番の色が黄色（境界線色）のまま | OCRの信頼度が基準に届かなかった文字です。輪郭としては色分けされているはずなので「地番」表示ボタンで確認してください |
| 地番の数字が間違っている | OCRの誤読の可能性があります。必ず元画像と照合してください（特に1と7） |
| DXF/JWCの色が全部同じに見える | Jw_cad側の「色を初期化する」設定を確認してください（上記9参照） |
| 方角矢印の位置がずれている | ページ左上を基準にした暫定位置です。ずれが大きい場合はご連絡ください |
| 地番をクリックしてもハイライトされない | 地番ハイライト機能は現在無効化されています（上記「4. 距離測定・PNG保存」冒頭の注記を参照） |
